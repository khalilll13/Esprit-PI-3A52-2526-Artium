when@dev:
    web_profiler:
        toolbar: true
        intercept_redirects: false

    framework:
        profiler:
            only_exceptions: false
            collect_serializer_data: false  # Disable to improve performance

when@test:
    framework:
        profiler: { collect: false }
