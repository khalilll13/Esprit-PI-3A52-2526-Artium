{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.contenu) }}
    {{ form_row(form.date_reponse) }}
    {{ form_row(form.reclamation) }}
    {{ form_row(form.user_admin) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
