[mysqld]
# Increase max packet size to handle large audio files (64MB)
max_allowed_packet=64M

# Optional: Increase buffer sizes for better performance with BLOBs
innodb_buffer_pool_size=256M
innodb_log_file_size=64M
